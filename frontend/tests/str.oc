func main() {
    int a[10];
    //print(a);
    //print(a[0]);
    int i = 10;
    int j = 0;
    while (i > 0) {
        a[j] = i;
        //print(a[j]);
        i = i - 1;
        j = j + 1;
    }
    while (i < 10) {
        print(a[i]);
        i = i + 1;
    }
    print(a[0]);
    print(a[2]);
    //print(100);
    insertion_sort(a);
}

func insertion_sort(int[] a) -> void {
    //print(a[1]);
    return;
}