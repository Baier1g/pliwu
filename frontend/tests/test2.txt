int yarr = 500;
main(5, "c");

func main(int argv, char argc) {
    print(yarr);
    int test;
    int habibi = 30;
    print(habibi / 10);
    bool sheeesh = id(habibi, 50, "f"); 

    // Nested function test
    func nest(int a) -> int {
        print(yarr);
        print(habibi);

        func nest_nest(int a) -> int {
            print(yarr);
            print(habibi);
            return a + 10;
        }

        return nest_nest(a) / 10;
    }
    print(nest(habibi));
    int i = 0;
    while (i < 5) {
        print(i);
        i = i + 1;
    }
    char lll = "l";
    bool fgg = false;
    return;
}

func id(int a, int b, char c) -> bool {
    int bombaclat = 1000;
    /*return 50 * 100 - 10 / 5 > 60 + 70 * 3 || a * b - 1002 / 3 > 2 * a - b && true || false;*/
    if (a > b) {
        bombaclat = bombaclat - 500;
    } else {
        bombaclat = bombaclat + 500;
    }
    return true;
}
