IDEAS:
1. include <func> (, <func>)*  from <module>
2. compression of machinecode

DELIBERATIONS:
1: Strings and chars: Should chars be handled like strings or should 
                      they be handled differently? Should chars be able
                      to handle the extended ascii set, UTF-8 and other
                      character sets as well in the language?

DECISIONS:
1. Arrays on the heap have elementsize, dimensionality, dimension sizes and num_elements declared.
   Base address will hold elementsize, base address + 8 will hold dimensionality, base_address + 16 and following quad qords will hold the sizes of each individual array,
   the first quad word after the last dimension size will hold the total number of elements and the following quadword will hold a reference count, for potential garbage collection. Elements start from the following quad word.
   TODO related to this: alloc function will now have to take all these as arguments, current idea is to pass elementsize and dimensionality in rdi and rsi and let individual dimension sizes be passed on the stack

TODOS:
1. find_keyword() (and find_type()) must be mapped or perfectly hashed
2. Restrict length of identifiers
3. Debug print for all modules during test
4. [ish] Proper error synchronization in parser
5. Find out how to properly import enums to grammar.y
6. Clean inputs on identifiers in rules.l (done??) (possibly better solution: Manually set '\0' character on strings)
7. Add filename to module in AST_node, to improve error handling clarity.
8. Hella memory leaks in codegen when writing lines with specific offsets (too bad!)
9. Der skal startes fra main og den må ikke kalde sig selv
10. Declaring unassigned variables not working in codegen
11. Add type to print statement (in ast node) ((so we can decide which assembly function to use easier))
12. Eliminate dead code, either in scope or later
13. Maybe make set
14. Constant folding on AST, potentially on variables if we do SSA form
15. [X] Potentially implement rewrite program in register_allocation
16. [X] Problems in rewrite_segment, variables end up in print statements
17. Scopechecker needs to check if a variable has been defined before use
18. Must disallow chaining relational expressions in grammar (5 < 6 < 7)
19. Infinite recursion on 3 registers in register allocation
20. Offset must be added to local variables in IR_codegen
21. Distinction between char and string at lexer/parser level is weak, can't do single character strings atm
22. Size of arrays are definable at runtime, maybe a bad idea

QUESTIONS:
1. Redundant moves due to register allocation not coalescing
T2 <- T2 + T1
T3 <- T2
2. How much memory leak is too much?
3. For tilfælde hvor en variabel er flyttet til et register, der bliver brugt flere gange 
kan den blive overskrevet uagtet da resultatet i f. eks. addition ender i source operand.
eg.
int a = 5;
int c = a+a;
print(a); //10
løsning: move source ind i rax og udføre operation, så mov resultat til det allokerede resultat register

IR calls:
Når der konverteres fra AST til IR skal værdierne i registrene R8-R15 gemmes. Hvis alle pushes hver gang kan det lede til et stort ydelsestab.
Derfor pushes kun funktionsparametrene (RSI, RDI, RCX, RDX) under konversionen til IR.
IR_operationen for at pushe vores caller save registre bliver skabt efter register allokering, så kun de registre der er i brug bliver pushet.

int a = 300;
int b = a - 500

T1 <- a
T2 <- 500
T3 <- T1
T4 <- sub T3, T2

BUGS: (Let's write bugs here (A bit late, I know, get off my back))
1. IR_VAR_DECL problems when assigning a local variable to a function parameter. Local var doesn't get its own temp and chains itself to the temp of the function parameter...
func function(int a) {
    int b = a;
    b = 1; (It puts 1 into a instead)
    int c = 1;
    int d = c;
    d = 1; (Same problem, this puts value 1 into c)
}
STATUS: Think I fixed it, not sure

FIXES:
1. Allow and implement string arrays
2. Fix char printer [EVERYTHING IS QUADWORDS]
3. Implement bool printer [Works now]
4. Fix _print_char_array [FIXED]
5. Implement multidimensional functionality for array printers
6. Don't print NULL characters in char arrays [FIXED]